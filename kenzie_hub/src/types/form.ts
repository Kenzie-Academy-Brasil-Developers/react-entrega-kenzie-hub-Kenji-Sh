export interface iUserFormValue {
  name: string;
  email: string;
  password: string;
  passwordConfirm?: string;
  bio: string;
  contact: string;
  course_module: string;
}

export interface iTechFormValue {
  title?: string;
  status: string;
}
